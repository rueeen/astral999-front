import './PaywallModal.css'
export default function PaywallModal({data,onClose}){if(!data)return null;return <div className="modal-backdrop" role="dialog" aria-modal="true"><div className="panel modal"><button className="modal-close" onClick={onClose} aria-label="Cerrar">×</button><span className="eyebrow">Límite del cosmos</span><h2>{data.code==='spread_not_available'?'Esta tirada requiere Premium':'Agotaste tus lecturas'}</h2>{data.used!=null&&<p>Has usado <strong>{data.used}</strong> de <strong>{data.limit}</strong> lecturas.</p>}{data.resets_at&&<p>Tu cupo se renueva el {new Date(data.resets_at).toLocaleDateString('es-ES')}.</p>}<button className="button" onClick={()=>alert('Mejoras de plan: próximamente')}>Mejorar mi plan</button></div></div>}

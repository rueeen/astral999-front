import './TarotCardImage.css'
const roman=(value)=>{const n=Number(value);if(!Number.isFinite(n))return value||'✦';if(n===0)return '0';const map=[['M',1000],['CM',900],['D',500],['CD',400],['C',100],['XC',90],['L',50],['XL',40],['X',10],['IX',9],['V',5],['IV',4],['I',1]];let x=n,out='';map.forEach(([r,v])=>{while(x>=v){out+=r;x-=v}});return out}
export default function TarotCardImage({card={},reversed=false}){const number=card.arcana_number??card.number??card.order;return <div className={`tarot-wrap ${reversed?'is-reversed':''}`}><div className="tarot-card">{card.image?<img src={card.image} alt={card.name||'Carta de tarot'}/>:<div className="tarot-placeholder"><span>{roman(number)}</span><i>✦</i><strong>{card.name||'Arcano'}</strong></div>}</div>{reversed&&<span className="reversed-label">Invertida</span>}</div>}
